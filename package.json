{
  "name": "pos-system-qa",
  "version": "1.0.0",
  "description": "Web-based POS System Quality Assurance Testing Suite",
  "main": "index.js",
  "scripts": {
    "test": "python -m pytest tests/ -v",
    "test:cart": "python3 -m pytest tests/test_cart.py -v",
    "test:login": "python -m pytest tests/test_cart.py::test_user_login_valid -v",
    "test:smoke": "python -m pytest tests/ -m smoke -v",
    "test:add": "python -m pytest tests/test_cart.py::test_add_item -v -s",
    "test:remove": "python -m pytest tests/test_cart.py::test_remove_item -v -s",
    "test:update": "python -m pytest tests/test_cart.py::test_update_quantity -v -s",
    "test:coverage": "python -m pytest tests/ --cov=pages --cov=tests --cov-report=html --cov-report=term",
    "clean": "rm -rf reports/ .pytest_cache/ __pycache__/ .coverage htmlcov/",
    "setup": "pip install -r requirements.txt",
    "help": "echo 'Available test commands:' && echo '  npm run test          - Run all tests (browser visible)' && echo '  npm run test:smoke     - Run smoke tests (browser visible)' && echo '  npm run test:cart      - Run all cart tests (browser visible)' && echo '  npm run test:add       - Run add item test (browser visible)' && echo '  npm run test:remove    - Run remove item test (browser visible)' && echo '  npm run test:update    - Run update quantity test (browser visible)' && echo '  npm run test:coverage  - Run with coverage report' && echo '  npm run clean          - Clean test artifacts' && echo '  npm run setup          - Install dependencies'"
  },
  "keywords": [
    "pos-system",
    "qa-testing",
    "selenium",
    "pytest",
    "web-automation"
  ],
  "author": "QA Team",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aldifhrent/fp-qa.git"
  },
  "bugs": {
    "url": "https://github.com/aldifhrent/fp-qa/issues"
  },
  "homepage": "https://github.com/your-org/pos-system-qa#readme"
}
